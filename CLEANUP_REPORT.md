# Unity 이전 파일 정리 기록

## 유지 기준

원본 입력, 현행 코드, 테스트, 재현에 필요한 중간자료, 최종 보고서, Unity 번들은
유지한다. 특히 `ptd_library_v2.3.ttl`과 `Appendix_PtD_Library_v2.3.docx`는 현행 v2.4
생성 파이프라인이 참조하므로 구버전처럼 보여도 삭제하지 않는다.

## 삭제 완료

- 루트·`scripts/`·`tests/`의 `__pycache__`
- `.pytest_cache`
- `build/_pytest_current`, `build/_pytest_tmp_v38`

모두 실행 시 자동 재생성되는 캐시 또는 테스트 임시물이다.

## `삭제_보관함`으로 이동 완료

아래 항목은 현행 실행에는 필요하지 않지만 복구·감사 가치가 있어 영구삭제하지 않고
저장소 루트의 `삭제_보관함/` 아래 원래 상대경로를 유지해 옮겼다.

- `삭제_보관함/data/backup_v3.5~v3.7`
- `삭제_보관함/`의 Unity 화면녹화 MP4 2개
- `삭제_보관함/`의 Unity `.meta` 5개
- `삭제_보관함/build/_diag`

어느 항목도 완전 삭제하지 않았으며 원래 위치로 이동하면 복구된다.
